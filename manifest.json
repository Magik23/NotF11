{
  "manifest_version": 3,
  "name": "Chromium Clean Window",
  "version": "0.1.0",
  "description": "Toggle live browser tabs between normal browser windows and clean popup windows.",
  "permissions": [
    "storage"
  ],
  "background": {
    "service_worker": "background.js"
  },
  "commands": {
    "toggle-clean-window": {
      "suggested_key": {
        "default": "Ctrl+Shift+F"
      },
      "description": "Toggle the focused tab between normal and clean window mode"
    },
    "next-clean-tab": {
      "suggested_key": {
        "default": "Ctrl+Shift+Period"
      },
      "description": "Show the next source tab in the focused clean window"
    },
    "previous-clean-tab": {
      "suggested_key": {
        "default": "Ctrl+Shift+Comma"
      },
      "description": "Show the previous source tab in the focused clean window"
    }
  }
}
